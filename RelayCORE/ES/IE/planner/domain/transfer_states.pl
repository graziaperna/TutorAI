%plan_states([at(a), use_transport(metro)], [at(m)]).

%plan_states([at(c), use_transport(bus), use_transport(metro)], [at(l)]).

plan_states([at(a), use_transport(bus), use_transport(metro)], [at(d)]).

